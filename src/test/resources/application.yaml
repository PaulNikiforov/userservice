spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  liquibase:
    enabled: true

  cache:
    type: redis

logging:
  level:
    com.innowise.userservice: DEBUG
